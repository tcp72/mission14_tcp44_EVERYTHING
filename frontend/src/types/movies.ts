export type Movie = {
  movieId: number;
  category: string;
  title: string;
  year: number;
  director: string;
  rating: string;
  edited: string;
  lentTo: string;
  notes: number;
};
//these need to match the names from C#
